#will execute the default values
def product(a=1,b=1,c=1):
    return (a*b*c)
#will execute the give values

print(product())
def product(a=1,b=1,c=1):
    return (a*b*c)
print(product(5))

#the values inside the brackets like "5" are given more prefrance